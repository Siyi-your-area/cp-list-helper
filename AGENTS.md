# CP List Helper Agent 协作指南

本文件是仓库级 Codex 指令。所有主 Agent 与子 Agent 在本仓库工作时都必须遵守。

## 项目目标

CP List Helper 是基于 Next.js 14、React 18、TypeScript、Tailwind CSS 与 Supabase 的展会心愿单工具。核心用户旅程是：

1. 创建或加入心愿单；
2. 上传 Excel/CSV；
3. 将用户条目与 CPP/CPG 数据匹配；
4. 在桌面端和手机端查看、编辑、筛选和排序；
5. 分享、删除和导出心愿单。

优先保证数据正确、自动匹配精确率、访问控制和移动端现场使用体验。不要以增加匹配数量为由降低安全接受阈值。

## 先读这些文件

- 产品与文档入口：`docs/00-index.md`
- 架构说明：`docs/04-architecture.md`
- 测试说明：`docs/05-testing.md`
- 数据同步：`docs/07-cpp-sync-runbook.md`
- 匹配现状与基线：`docs/08-matching-current-state.md`
- 产品验收与数据关系：`docs/09-product-cpp-data-matching-guide.md`
- 首页：`app/page.tsx`
- 详情页：`app/exhibit/[id]/page.tsx`
- 手机视图：`components/MobileTableView.tsx`
- 匹配引擎：`lib/cpp-matcher.ts`
- 数据访问：`lib/db-service.ts`
- Excel 解析：`lib/excel-parser.ts`
- 数据库初始化与迁移：`docs/init-db.sql`、`docs/migrations/`

文档可能滞后于代码。发现不一致时，以实际代码和可复现验证为证据，并在报告中明确指出差异。

## 通用工作规则

- 开始前运行 `git status --short`。工作区中的现有修改属于用户或其他任务；不得重置、覆盖、暂存、移动或清理它们。
- 并行编码必须使用独立 Git Worktree。不要让两个产品代码写入者同时在同一工作目录工作。
- 审查与方案 Agent 默认只读；只有 Agent3 可以修改产品代码。Agent4 只有在主 Agent 明确授权时才能修改测试、fixture 或测试文档，不得修改产品实现来让测试通过。
- 不执行生产部署，不运行破坏性数据库操作，不把生产数据库当测试库，不提交密钥、Cookie、Service Role Key 或真实用户数据。
- 数据库变更必须新增迁移，保持向后兼容，并包含部署顺序、回填、验证和回滚方案。不得原地改写已执行迁移。
- 修改 API、数据库字段或匹配结果结构时，必须检查首页导入、详情页导入、持久化、刷新恢复、分享访问和导出链路。
- 不要直接修改匹配权重、阈值、标准化、摊位拆分或别名规则。任何调整必须先扩充正负样本，并用质量与性能基线证明没有回归。
- 不运行 `sync:cpp` 的真实写入模式、外部 CPP 请求或线上迁移，除非用户明确授权。优先使用 fixture、`--dryRun` 和隔离测试环境。
- 不主动提交或推送 Git；只有用户明确要求时才执行。

## 四个项目 Agent

项目级自定义 Agent 位于 `.codex/agents/`：

- `agent1_experience`：UI/UX、响应式、无障碍、文案、数据与版权声明方案。
- `agent2_architect`：匹配、导入、数据库、安全和可观测性的架构方案。
- `agent3_implementer`：依据已确认方案实施代码，是唯一产品代码写入者。
- `agent4_tester`：建立测试基线、设计用例、独立执行回归并给出发布判断。

不要让 Agent1 或 Agent2顺手写代码，也不要让 Agent3 自己代替 Agent4 给出最终通过结论。

## 推荐编排流程

复杂的 UI、匹配、导入或数据库优化使用以下阶段；小型文档或单文件修正不必机械启动全部 Agent。

### 阶段 0：主 Agent 冻结任务边界

1. 确认当前分支、工作区状态和是否需要独立 Worktree；
2. 写清本轮目标、非目标、可修改文件、不可修改行为与验收指标；
3. 记录当前代码与文档的已知差异；
4. 不清理当前工作区，不假设未提交文件可以丢弃。

### 阶段 1：并行只读分析

主 Agent 可并行启动：

- Agent1：完成体验审查与 UI 验收标准；
- Agent2：完成架构审查、ADR、迁移和兼容方案；
- Agent4：把需求转成测试矩阵，并记录可执行的当前基线。

三者只返回证据、方案、风险与验收标准，不修改产品代码。主 Agent 等待所有结果后解决冲突并形成一份实施计划。

### 阶段 2：实施门禁

只有满足以下条件才启动 Agent3：

- Agent1/Agent2 的相关方案已汇总；
- Agent4 已定义可验证的验收条件；
- 数据库迁移、API 契约与匹配阈值的风险已明确；
- 已划定 Agent3 的文件范围；
- 当前 Worktree 不会与其他正在写入的任务冲突。

Agent3 按计划实施。UI 与核心架构均为大改时，应分成可独立验证的连续批次，避免一次混改导致无法定位回归。

### 阶段 3：独立回归

Agent3 完成后，由 Agent4 独立执行回归。失败时：

- 实现缺陷退回 Agent3；
- 体验需求不清退回 Agent1；
- 架构或迁移歧义退回 Agent2；
- 不得通过删除测试、放宽阈值或隐藏错误来通过门禁。

主 Agent 最终汇总已完成项、未完成项、测试证据、性能指标和剩余风险。

## Agent1 验收重点

- 覆盖首页、创建/导入、详情、搜索筛选、编辑、状态切换、分享、删除和导出。
- 分别考虑手机现场使用与桌面整理场景，至少检查 320、375、390、768、1024 和 1440 px。
- 主要触控目标原则上不小于 44×44 CSS px；页面不得出现阻断操作的溢出、遮挡或软键盘裁切。
- 图标按钮有可访问名称；弹窗/抽屉具备正确语义、焦点管理、Esc 关闭与焦点恢复；关键流程可用键盘完成。
- 明确中文字体栈、字号、行高、字重、间距、颜色、圆角和交互状态。动画尊重 `prefers-reduced-motion`。
- 设计 loading、empty、error、success、disabled、partial import、unsaved、无权限和弱网状态，减少只依赖 `alert/confirm` 的反馈。
- 数据与版权声明只能基于已确认事实：应区分第三方数据来源、自动匹配局限、非官方关联、用户上传内容、设备识别、分享可见性与删除语义。不得虚构授权、法律结论或隐私承诺。
- UI 改动不得改变匹配结果、导入字段、数据库结构或 API 行为，除非这些变化已进入 Agent2 方案并被明确批准。

## Agent2 架构重点

- 评估同步脚本与运行时匹配之间的标准化、别名和摊位拆分规则是否漂移。
- 评估迁移 005 的实际部署状态，以及 normalized 字段、`pg_trgm`/RPC Top-K 和索引方案；所有依赖新字段的代码必须有迁移门禁。
- 设计导入事务性、幂等键、失败恢复与部分完成契约。
- 澄清 `wish_items.cpp_item_id` 实际指向 `cpp_items.doujinshi_id` 的语义；关联键变更必须采用双写、回填、校验、切读和清理阶段。
- 将用户原始输入、匹配决策、候选、评分与 CPP 快照分离，避免把机器状态只编码进 `note`。
- 审计 Supabase RLS、owner/viewer/stranger 权限和前端 anon 直连边界；授权必须由数据库策略、RPC 或服务端接口强制执行。
- 设计长期匹配指标、外部接口超时/预算、同步状态和失败报告的可观测性。
- 将文档、实现和黄金集对齐；发现阈值或歧义处理不一致时先报告，不直接调参。

## Agent3 实施规则

- 开始前复述已批准范围、验收标准和禁止项，并再次检查工作区状态。
- 只实现主 Agent 明确交付的计划，不自行扩大到重构整个系统。
- 保持现有技术栈与代码风格；优先抽取小型可测试模块，避免继续扩大超大 Client Component。
- UI 实施应减少桌面/手机重复业务逻辑，并建立最小设计 token/基础组件，但不得为统一样式重写无关页面。
- 匹配实施必须保留高精确率优先原则；不接受以召回率换误匹配率的无证据改动。
- 数据库实施只新增向后兼容迁移，不应用到生产环境。
- 对修复的缺陷补充永久回归用例；如果仓库缺少所需测试基础设施，先提交最小可维护方案给主 Agent确认。
- 完成后提供文件清单、行为变化、执行命令、测试结果、未执行验证与剩余风险。

## Agent4 测试与发布验证

Agent4 不实现产品功能，负责把 Agent1 和 Agent2 的方案转换为验收用例，并独立验证 Agent3 的实现。

每次开始测试前记录基线。每个缺陷修复应增加永久回归用例。测试失败时提供命令、环境、复现步骤、期望结果、实际结果和相关日志，不得自行降低验收标准。

当前仓库没有正式单元测试框架、统一 `test` 命令或可用的仓库内 E2E 脚本；`npm run lint` 目前可能进入初始化交互，不能假设它是自动化门禁。搭建测试基础设施后应同步更新本文件。

最低验证：

1. 所有代码改动：
   - `npx.cmd tsc --noEmit --incremental false`
   - `npm.cmd run build`
2. 匹配逻辑改动：
   - `npm.cmd run evaluate:matching`
   - 完整 API 匹配评估；
   - 真实或等价规模 wishlist benchmark；
   - 自动匹配精确率不低于 95%；
   - 误匹配率不得高于基线；
   - 召回率不得低于测试开始时记录的基线；
   - 端到端 P95 不高于 30 秒。
3. UI 改动：
   - 覆盖 375、390、768、1280、1440 px；
   - 回归创建、加入、导入、编辑、搜索、排序、删除和导出；
   - 检查键盘、焦点、弹窗、标签、颜色对比、触控尺寸和横竖屏溢出。
4. 数据库、访问控制或分享改动：
   - 只在隔离测试库验证；
   - 覆盖 owner、viewer、stranger；
   - 验证重复邀请幂等、viewer 移除/重新加入、owner 删除及旧邀请码失效。
5. 发布前：
   - 生产构建；
   - 测试环境冒烟；
   - 桌面与手机核心用户旅程；
   - 未获用户明确授权不得部署。

Agent4 最终报告必须列出：执行命令、通过项、失败项、未执行项、质量与性能指标、测试环境和剩余风险。

## 当前质量基线

以实际运行结果为准，不把文档中的历史数字当永久门槛。每次任务开始时重新记录：

- 黄金集样本数量；
- 自动匹配精确率、召回率、误匹配率；
- 离线与完整 API 的 P50/P95；
- TypeScript 与构建结果；
- 关键桌面/手机页面状态。

如果基线因环境、网络、数据库或 Cookie 不可复现，必须明确写成“未验证”，不得声称通过。

