# CP List Helper 开发规则

本文件适用于仓库内所有 Codex Agent。目标是在保证数据正确、访问安全和移动端可用的前提下，用最简单、可持续的方式交付产品。

## 产品目标

CP List Helper 基于 Next.js 14、React 18、TypeScript、Tailwind CSS 和 Supabase。核心流程是：创建或加入 list、上传 CPP Excel、匹配 CPP/CPG 展品、编辑与排序、分享协作、现场标记购买状态及导出。

优先级依次为：数据正确与访问控制、自动匹配精确率、核心流程可用、移动端体验、性能与开发效率。不得为了提高匹配数量降低自动接受的可靠性。

## 开发原则

- **不维护废弃路径的向后兼容。** 已确认废弃的代码、兼容层、回退逻辑和旧配置应直接删除。仍在使用的数据与功能不得未经确认破坏。
- **简单优先。** 满足当前需求即可；如无实际需求，不新增实体、抽象、配置项、中间层或“以后可能用到”的扩展点。
- **渐进交付。** 先完成可端到端运行的最小版本，再从稳定产品上逐步增加功能。不要用尚未成熟的复杂方案替换已经可用的流程。
- **职责清晰。** 保持组件和模块边界明确；页面负责交互编排，业务规则放入可复用模块，数据访问集中管理，脚本不要混入产品 UI 逻辑。
- **优先使用成熟能力。** 现有依赖或成熟库能降低复杂度、提高可靠性时优先采用。新增依赖或自行实现前，先阅读现有依赖的官方文档和类型定义。
- **面向长期演进。** 不采用已知很快要被替换的临时方案；长期设计仍应从最小可用实现开始，而不是一次引入完整平台化架构。
- **不过度防御。** 校验应针对真实风险和已出现的问题。不要增加多轮重复扫描、哈希审批、复杂基线、恢复工件或繁重门禁，除非用户明确要求或存在不可接受的数据损失风险。
- **证据适量。** 验证规模与改动风险匹配。用户要求先人工检查时，完成必要检查后立即停下，不自行扩大测试范围。

## 开始工作

1. 运行 `git status --short`，保留用户和其他任务的现有修改；不得重置、覆盖、移动或清理无关改动。
2. 阅读与任务直接相关的代码和文档，不为简单任务机械加载全部资料。
3. 明确本轮目标、非目标和验收条件；能够从现有代码确认的信息不再要求用户重复提供。
4. 文档可能滞后，以实际代码和可复现行为为准；发现差异时顺手修正文档或明确记录。

常用入口：

- 产品与文档：`docs/00-index.md`
- 架构：`docs/04-architecture.md`
- 测试：`docs/05-testing.md`
- CPP/CPG 同步：`docs/07-cpp-sync-runbook.md`
- 匹配：`docs/08-matching-current-state.md`
- 首页：`app/page.tsx`
- list 详情：`app/exhibit/[id]/page.tsx`
- 手机视图：`components/MobileTableView.tsx`
- 匹配引擎：`lib/cpp-matcher.ts`
- 数据访问：`lib/db-service.ts`
- Excel 解析：`lib/excel-parser.ts`
- 数据库：`docs/init-db.sql`、`docs/migrations/`

## 数据与安全

- 不提交 Cookie、Secret、Service Role Key、`.dev.vars`、真实用户数据或本地运行日志。
- 未经用户明确授权，不部署生产、不写生产数据库、不执行外部 CPP 请求、不提交或推送 Git。
- 数据库结构变更必须新增迁移；已经执行的迁移不得原地改写。除非当前产品明确需要，不为旧代码路径设计兼容迁移。
- 生产同步只允许修改 `cpp_items` 目标展会数据，不得修改用户的 `events`、`wish_items`、成员关系或历史 list。
- CPG 同步以 `event_id + day_id + doujinshi_id` 作为唯一身份；新增记录插入，已有记录按字段有效性更新，不因来源暂时缺失删除数据。
- `wish_items.cpp_item_id` 当前保存 `cpp_items.doujinshi_id`，修改该语义前必须明确评估导入、持久化、刷新、分享和导出链路。
- RLS、权限和分享规则必须由数据库或服务端强制执行，不能只依赖前端隐藏操作。

## 匹配规则

- 不直接修改匹配权重、阈值、标准化、摊位拆分或别名规则。
- 必须调整时，先加入能够复现需求的正负样本，再运行现有匹配评估；自动匹配精确率不得低于 95%，误匹配率不得高于改动前。
- 数据完整性与匹配算法质量分开判断；外部 CPP 兜底不能掩盖本地数据缺失，也不能自动写回 `cpp_items`。

## CPG 数据更新

- 每次只扫描 CPG08 当前一期和页面实际存在的 16 个类目；类目与总数来自 CPP 当前响应，不把历史展示数字写死。
- 使用一次完整分页扫描即可。扫描期间新增的展品允许在下一轮补齐；单页失败可有限重试，整轮失败则停止写入。
- 新 `doujinshi_id` 直接补充；已有记录只更新来自 CPP 的有效字段；来源未出现的旧记录保留。
- `author`、`booth_number`、`booth_name`、`image_url`、`description`：CPP 返回非空新值时覆盖；返回空值时保留数据库原值。
- `exchange_type`：只有识别为明确的“有偿交换”或“无料交换”时覆盖；无法识别时保留原值。
- `hot_count`：返回非负数字时更新，包括 `0`。
- `product_name`、`type_id`、`type_name`、`tags`、`original_work`、`source_url`：返回结构有效时更新。
- 标准化字段和别名从本轮最终值重新计算。同步不依赖哈希审批或多轮一致性基线。

## 实施与验证

- 只修改当前需求涉及的文件；不要顺手重构无关模块。
- API、字段或匹配结果结构变化时，检查首页导入、详情页导入、保存、刷新、分享和导出链路。
- 普通代码改动通常运行 TypeScript 检查和构建；纯脚本或文档改动只做与其风险相称的语法检查或小样本验证。
- UI 改动重点检查 375、390、768、1280 和 1440 px，避免遮挡、横向溢出和小于 44×44 CSS px 的主要触控目标。
- 生产数据任务先输出简洁的新增、更新、跳过、失败统计。用户要求人工检查时，到此停止。

## Git 与协作

- 并行写代码时使用独立 Git Worktree，避免多个写入者修改同一目录。
- 审查 Agent 默认只读；实施 Agent 只改获批范围；测试 Agent 不通过修改产品实现让测试通过。
- 不主动暂存、提交、推送或创建 PR。只有用户明确要求时执行，并确保排除日志、密钥、Cookie 和真实数据。
