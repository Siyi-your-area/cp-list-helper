# CP list帮手用户旅程测试清单（2026-07-15）

## 覆盖范围

本测试覆盖从创建 list、导入心愿单、匹配 CPP 数据、编辑、排序、导出、多端同步、删除 list 到部署前检查的核心用户旅程。

## 已执行检查

- TypeScript 类型检查：通过
  - 命令：`npx tsc --noEmit --incremental false`
- 依赖完整性：通过
  - `exceljs` 已写入 `package.json` 和 `package-lock.json`
- 生产构建：未完成
  - `next build` 在本机 Codex 沙箱中 3 分钟未结束，无编译错误输出
- ESLint：未执行
  - 项目尚未配置 ESLint，`next lint` 会进入交互式初始化

## P0 核心流程

| 用例 | 前置条件 | 步骤 | 预期结果 | 状态 |
|---|---|---|---|---|
| 创建 CP32 一期 list | Supabase 可访问 | 首页点击创建新展会，选择 CP32 一期 | 首页出现 CP32 一期卡片 | 待浏览器验证 |
| 创建 CP32 二期 list | Supabase 可访问 | 首页点击创建新展会，选择 CP32 二期 | 首页出现 CP32 二期卡片 | 待浏览器验证 |
| 上传 Excel 并匹配 | list 页面为空 | 上传测试 Excel | 显示等待文案，结束后出现导入成功弹窗 | 待浏览器验证 |
| CPP 匹配正确性 | Excel 中含已知摊位/商品 | 导入后检查图片、作者、热度、详情 | 匹配结果被写入心愿单 | 待浏览器验证 |
| 邀请码复制与加入 | list 有分享码 | 复制邀请码，在首页输入 | 跳转到同一 list 页面 | 待浏览器验证 |
| 手机访问同一 list | 手机与电脑同网或已部署 | 手机打开同一站点并输入邀请码 | 手机看到同一份数据 | 待真机验证 |

## P1 编辑与管理

| 用例 | 步骤 | 预期结果 | 状态 |
|---|---|---|---|
| 单行编辑 | 进入编辑模式，修改状态/价格/数量/备注 | 数据保存并刷新后仍保留 | 待浏览器验证 |
| 单行删除 | 编辑模式删除单行 | 该行消失，刷新后不恢复 | 待浏览器验证 |
| 多选批量删除 | 编辑模式勾选多行，点击删除选中 | 选中行全部删除，未选中行保留 | 待浏览器验证 |
| 退出编辑模式 | 编辑模式下选中多行后退出 | 选择状态清空 | 待浏览器验证 |
| 删除整份 list | 首页点击 list 删除按钮 | 内部弹窗确认，删除后卡片消失，邀请码失效 | 待浏览器验证 |

## P1 排序与查看

| 用例 | 步骤 | 预期结果 | 状态 |
|---|---|---|---|
| 默认排序 | 不选择排序按钮 | 按摊位号自然排序 | 待浏览器验证 |
| 热度排序 | 点击按热度排序 | 按热度降序，热度相同按摊位号 | 待浏览器验证 |
| 取消热度排序 | 再次点击按热度排序 | 回到摊位号排序 | 待浏览器验证 |
| 优先级排序 | 点击按优先级排序 | 按首摊/次摊/P1/P2/P3/随缘排序 | 待浏览器验证 |
| 取消优先级排序 | 再次点击按优先级排序 | 回到摊位号排序 | 待浏览器验证 |
| 查看详情 | 点击查看详情 | 弹窗展示详情长文本，表格不被撑宽 | 待浏览器验证 |

## P1 导出

| 用例 | 步骤 | 预期结果 | 状态 |
|---|---|---|---|
| 导出 Excel | 点击导出 | 下载 `.xlsx` 文件 | 待浏览器验证 |
| 图片嵌入 | 打开导出的 Excel | 可访问图片会作为真实图片嵌入；跨域读取失败的图片保留图片链接 | 待本机 Excel 验证 |
| 长文本导出 | 检查详情列 | 详情文本完整导出并自动换行 | 待本机 Excel 验证 |

## P2 异常场景

| 用例 | 步骤 | 预期结果 | 状态 |
|---|---|---|---|
| 重复导入 | 再次上传同一个 Excel | 重复项被跳过，弹窗显示跳过数 | 待浏览器验证 |
| 空 Excel | 上传空文件 | 提示没有找到数据 | 待浏览器验证 |
| 匹配服务失败 | 断网或 Supabase 不可用后导入 | 不崩溃，未匹配模式保存基础字段 | 待浏览器验证 |
| 邀请码错误 | 输入不存在的邀请码 | 显示邀请码无效 | 待浏览器验证 |

## 部署前必须确认

- 生产环境变量：
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- Supabase 表结构：
  - `events.share_code`
  - `events.cpp_event_id`
  - `wish_items.hot_count`
  - `wish_items.description`
  - `cpp_items.day_id`
- 图片存储策略：
  - 当前导出会尝试嵌入可读取图片
  - 后续建议改为 Supabase Storage，数据库只保存 URL
